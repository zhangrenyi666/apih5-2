// 常量声明
export const { dev, domain, appInfo, storage, wxSdk, apis } = window.configs
export const wx = window.wx
export const WwLogin = window.WwLogin
export const URI = window.URI
const MyPublic = {
    dev,
    domain,
    appInfo,
    storage,
    wxSdk,
    apis,
    wx,
    WwLogin,
    URI
}
export default MyPublic